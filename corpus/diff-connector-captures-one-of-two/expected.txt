warning schema-before-connector diff-connector-captures-one-of-two/migrations/0141_movement_doubt_cleared.sql:7
  this change adds public.reports.movement_cleared_by to a captured table without adding it to column.include.list in diff-connector-captures-one-of-two/connector.json (compared with base)
  the column will not be in the stream; if a sink is later given it, every row will be the default until a snapshot
  fix: add public.reports.movement_cleared_by to column.include.list in the same change, or, if it is left off on purpose, say so on the line that adds it: -- cdclint:ignore schema-before-connector: <why>
0 error(s), 1 warning(s), 0 info
