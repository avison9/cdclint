info source-column-not-captured diff-adds-column-connector-untouched/migrations/0033_report_validations.sql:7
  public.report_validations.assigned_by_user_id exists and is not matched by column.include.list in diff-adds-column-connector-untouched/connector.json; nothing reads it yet
  the day a sink asks for it is the day it is found missing
warning schema-before-connector diff-adds-column-connector-untouched/migrations/0035_validation_response_distance.sql:5
  this change adds public.report_validations.response_distance_m to a captured table without adding it to column.include.list in diff-adds-column-connector-untouched/connector.json (compared with base)
  the column will not be in the stream; if a sink is later given it, every row will be the default until a snapshot
  fix: add public.report_validations.response_distance_m to column.include.list in the same change, or, if it is left off on purpose, say so on the line that adds it: -- cdclint:ignore schema-before-connector: <why>
0 error(s), 1 warning(s), 1 info
